namespace queue_management.Enums
{
    public enum AgentStatus
    {
        // Agente No Disponible (no visible en consultas normales)
        Unavailable  = 0,
        // Agente  Disponible
        Available = 1,
        // Agente En Pausa
        Pausa = 2,
    }
}